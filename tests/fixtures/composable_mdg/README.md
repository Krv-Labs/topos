# COMPOSABLE GitNexus fixture

Minimal Rust sources used by the `composable` CI job to assert that
`topos evaluate` scores COMPOSABLE against a GitNexus ≥ 1.5 LadybugDB
binary store (issues #197 / #198).

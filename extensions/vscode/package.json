{
  "name": "topos-vscode",
  "displayName": "Topos: Code Quality Targets for Agents",
  "description": "Structural code-quality targets your coding agents can optimize toward, delivered as MCP tools for agent mode.",
  "version": "0.3.0",
  "publisher": "KrvLabs",
  "icon": "icon.png",
  "license": "SEE LICENSE IN LICENSE",
  "repository": {
    "type": "git",
    "url": "https://github.com/Krv-Labs/topos.git",
    "directory": "extensions/vscode"
  },
  "homepage": "https://github.com/Krv-Labs/topos",
  "bugs": {
    "url": "https://github.com/Krv-Labs/topos/issues"
  },
  "qna": false,
  "engines": {
    "vscode": "^1.120.0"
  },
  "extensionKind": [
    "workspace"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "copilot",
    "agents",
    "code-quality",
    "static-analysis",
    "topos"
  ],
  "categories": [
    "Linters",
    "Machine Learning",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:topos.generateDependencyGraph",
    "onCommand:topos.evaluateProject"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "mcpServerDefinitionProviders": [
      {
        "id": "topos-mcp",
        "label": "Topos"
      }
    ],
    "commands": [
      {
        "command": "topos.evaluateProject",
        "title": "Evaluate Project",
        "category": "Topos"
      },
      {
        "command": "topos.generateDependencyGraph",
        "title": "Generate Dependency Graph",
        "category": "Topos"
      }
    ],
    "configuration": {
      "title": "Topos",
      "properties": {
        "topos.executablePath": {
          "type": "string",
          "default": "",
          "description": "Custom path to the Topos CLI executable."
        },
        "topos.autoDiscover": {
          "type": "boolean",
          "default": true,
          "description": "Automatically discover Topos CLI in the active Python virtual environment."
        },
        "topos.autoDownload": {
          "type": "boolean",
          "default": true,
          "description": "Download and verify the standalone Topos binary when no bundled, cached, or local installation is found."
        },
        "topos.evaluatePath": {
          "type": "string",
          "default": "",
          "description": "Directory to pass to `topos evaluate` from **Topos: Evaluate Project**. Empty uses `src/` when present, otherwise the workspace root."
        },
        "topos.evaluateLanguage": {
          "type": "string",
          "default": "auto",
          "description": "Language for **Topos: Evaluate Project**. Use `auto` (default) to scan the evaluate path and run one evaluation per detected language (python, rust, javascript, typescript, cpp). Set a single language to restrict the run."
        },
        "topos.evaluatePreferences": {
          "type": "string",
          "default": "",
          "description": "Optional comma-separated pillar ranking for evaluation (e.g. simple,composable,secure). Passed as --preferences."
        },
        "topos.evaluateVerbose": {
          "type": "boolean",
          "default": true,
          "description": "Show a per-file medal and pillar breakdown in the evaluate terminal (topos evaluate -v)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm run check-types && npm run lint && node esbuild.js --watch",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "stage:binary": "node scripts/stage-binary.js",
    "stage:binary:clean": "node scripts/stage-binary.js clean",
    "package:vsix": "npm run package && npx --yes @vscode/vsce package",
    "package:darwin-arm64": "npm run package && npm run stage:binary -- darwin-arm64 && npx --yes @vscode/vsce package --target darwin-arm64 --out topos-vscode-darwin-arm64.vsix && npm run stage:binary:clean",
    "package:darwin-x64": "npm run package && npm run stage:binary -- darwin-x64 && npx --yes @vscode/vsce package --target darwin-x64 --out topos-vscode-darwin-x64.vsix && npm run stage:binary:clean",
    "package:linux-arm64": "npm run package && npm run stage:binary -- linux-arm64 && npx --yes @vscode/vsce package --target linux-arm64 --out topos-vscode-linux-arm64.vsix && npm run stage:binary:clean",
    "package:linux-x64": "npm run package && npm run stage:binary -- linux-x64 && npx --yes @vscode/vsce package --target linux-x64 --out topos-vscode-linux-x64.vsix && npm run stage:binary:clean",
    "publish:targets": "npx --yes @vscode/vsce publish --target darwin-arm64 darwin-x64 linux-arm64 linux-x64",
    "check-types": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "npm run test:staging && npm run test:unit",
    "test:staging": "node scripts/test-stage-binary.js",
    "test:unit": "tsx --test src/test/unit/*.test.ts",
    "compile-tests": "tsc -p tsconfig.test.json",
    "test:integration": "npm run package && npm run compile-tests && vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.x",
    "@types/vscode": "^1.105.0",
    "@typescript-eslint/eslint-plugin": "^7.14.1",
    "@typescript-eslint/parser": "^7.14.1",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.4.0",
    "esbuild": "^0.28.0",
    "eslint": "^8.57.0",
    "tsx": "^4.19.2",
    "typescript": "^5.4.5"
  },
  "overrides": {
    "serialize-javascript": "^7.0.5",
    "diff": "^9.0.0"
  }
}

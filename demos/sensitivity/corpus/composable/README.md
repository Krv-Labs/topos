# COMPOSABLE corpus

Pinned multi-module package slices for the COMPOSABLE pillar sensitivity sweep.

Regenerate with:

```bash
uv run python demos/sensitivity/curate.py
```

Requires `gitnexus` on PATH for coupling baselines. Package trees under
`funcy/` and `toolz/` are copied from PyPI and are not checked into git.
